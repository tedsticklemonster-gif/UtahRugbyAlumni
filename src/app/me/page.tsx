// Transitional: /me renders the profile page until the full Me tab
// (profile + giving + settings) lands. /profile redirects here via next.config.ts.
export { default, dynamic, metadata } from "../profile/page";
