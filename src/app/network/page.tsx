// Transitional: /network renders the directory until the merged Network tab
// (people + jobs) lands. /directory redirects here via next.config.ts.
export { default, dynamic, metadata } from "../directory/page";
